{% extends "base.html.twig" %}
{% block body %}
    <div style="padding:2em;">
        <div>Home Page</div>
        {% if isLoggedin == false %}
            {{loadComponent('login-form', [isLoggedin])}}
        {else}
        {% endif %}
    </div>
{% endblock body %}